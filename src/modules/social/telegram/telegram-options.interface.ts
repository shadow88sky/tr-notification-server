export interface TelegramOptions {
  /** token */
  token: string;

  options: Object;
  //   /** polling=false Set true to enable polling or set options. */
  //   polling?: boolean;

  //   /** Options which will be added for all requests to telegram api. */
  //   request?: Object;
}
